# Redis 工作区

Redis 插件连接完成后，在插件详情的连接操作旁点击「打开 Redis 工作区」。提供固定 Logical DB、已登记 Key 范围内的浏览，以及桌面人工 String/JSON 新增编辑和单 Key 删除；不提供切库、任意命令控制台或通配符批量删除。

## 浏览与查看

- 树上方展示固定 DB、当前 Key 范围、新增 Key 与刷新、视图切换、更多工具；搜索框单独一行。窄侧栏允许操作区换行，保持文字按钮可见。只有多个已登记范围时才显示范围切换菜单，单个范围直接展示。配置为 `*` 时显示「全部 Key（*）」，没有范围时不会自动扩大授权。
- 搜索区分大小写，普通关键词按包含匹配；支持 `*` 匹配任意长度字符、`?` 匹配单个 Unicode 字符，反斜杠转义通配符。未包含未转义的 `*` 时自动按包含匹配，例如 `user` 与 `*user*` 都能找到 `cache:user:1`，`user:*` 只匹配此前缀。`[]` 按字面字符处理，不解释为字符集合。勾选「精确匹配」后直接定位完整 Key，仍检查当前范围。
- 搜索框聚焦时显示最近搜索，输入仅筛选本地历史，不自动访问 Redis；可用上下方向键和 Enter，或点击候选项直接复用，并恢复当时的精确/模糊模式。当前工作区最多保留 20 条记录，按项目、环境、插件及已登记范围隔离，可清空当前范围记录。历史仅驻留内存，返回详情后保留，关闭工作区或会话失效后清除。
- 右键目录选择「搜索此目录下的 Key」，或选中目录后从更多工具选择「搜索所选目录」，自动填入该目录前缀并开始模糊搜索。目录名中的 `*`、`?` 和反斜杠会先转义，仍限定在当前已登记范围内。
- Enter 或搜索按钮提交一次后自动跨越空批次，结果逐步显示，累计新增约 500 个不同 Key 后暂停；匹配不足一页时继续到扫描完成。点击「继续搜索」从当前位置加载下一页。只有游标结束且结果全部保留才显示「搜索完成」。
- 搜索过程中可点击「停止搜索」；当前在途批次返回后保留结果和最新游标，不再发起下一批。输入新词、切换匹配方式或返回详情也会停止自动续扫，但不会自动提交新搜索。重新提交、刷新或切换 Key 范围会从头开始，旧查询的迟到结果不能进入新查询。
- Key 默认按冒号 `:` 分层显示为目录树，支持目录展开/折叠和完整名称列表切换。「更多工具」菜单提供定位当前 Key、全部展开/折叠和收起 Key 列表。叶子显示完整 Key，保留末尾冒号，悬停和底部路径栏也显示完整名称；同名 Key 和目录分别保留。目录数字只统计已加载的 Key。
- 搜索结果保留层级并展开匹配路径，继续扫描会合并到原目录并保留展开状态；「定位当前 Key」可展开路径并滚动到已加载的当前 Key。方向键、Home、End 导航目录，Enter 打开 Key，Ctrl / Command + Enter 固定标签。异常深层 Key 的前 12 层显示目录，叶子仍显示完整 Key。
- SCAN 自动按批次继续，只显示已加载数量，不显示推算的完成百分比。空批次不表示完成；返回游标结束才表示本轮完成。外部数据可能变化，不保证静态快照。列表不逐项查询类型或 TTL。
- 单击 Key 使用临时预览标签，双击固定，最多 8 个标签。每个标签独立保留显示方式和滚动位置。
- String 自动识别 JSON，支持文本、JSON 格式化及十六进制；Hash 显示字段和值并支持精确字段读取；List 显示索引和值；Set 显示成员；ZSet 显示从零开始的排名、成员和分数。其他类型只显示类型、TTL 等可用摘要。
- TTL 是读取时的采样值，区分剩余时间、不过期和不存在，不自动轮询。刷新 Key 会重新读取元数据和内容。
- 内容区顶部将类型标签、Key 名称与「复制 Key」图标集中排列；大小或成员数、TTL、读取时间位于下一行，刷新图标紧邻采样时间。文本、JSON、十六进制使用分段切换，旁边的「复制内容」复制当前显示内容；截断时明确显示「复制已加载部分」。数据以纯文本渲染，JSON 不执行代码或 HTML，也不会打开内容中的链接。
- Value 参考 ARDM 的结构化显示：JSON 语法高亮、逐层折叠、全部折叠/展开、默认自动换行，并支持在已加载内容内查找。内容获得焦点时 Ctrl / Command + F 查找 Value，其他位置仍聚焦 Key 搜索。查看器只读，操作显示控件不会访问 Redis。
- JSON 格式化仅调整字符串外空白，保留大整数、小数表示、重复字段及原始转义；复制包含折叠部分。截断且以对象或数组开头的值可格式化已加载的 JSON 片段，并明确提示读取字节数和末尾可能未闭合，不补齐数据。完整但非法的 JSON 保持原文显示。插件读取上限保持不变，需要完整读取时由用户调整插件配置后刷新。
- 只读查看器使用 CodeMirror 的 JSON 解析、折叠、高亮和视口渲染，避免大量内容一次创建全部行节点；不引入执行脚本或写 Redis 能力。格式化文本超过 2 MiB 或节点片段数量过多时回退到原文，缩进最多显示 32 层。
- 左侧栏可拖动调整或折叠，Ctrl / Command + F 聚焦 Key 搜索；标签支持方向键、Home、End 和 Delete。

## 会话与限制

「返回详情」隐藏工作区并保留数据，隐藏期间不发起后续读取；点击「返回 Redis 工作区」继续浏览。「关闭工作区」确认后清除数据和读取通道，插件主连接保持。切换插件保留工作区，最多保留 4 个 Redis 工作区。断连时保留有编辑草稿的工作区并禁用保存；重连需要重新读取核对。配置修订、删除所属资源和窗口销毁会失效旧会话，迟到请求不能恢复它。

桌面默认单次 IPC 返回最多 100 个 Key，集合每批最多 50 条，均受插件 `maxKeys` 限制；列表最多保留 5,000 个 Key。内容预览沿用 `maxValueBytes`（默认 64 KiB），超大 Hash 单字段不读取正文。UTF-8 截断保留字符边界；二进制 Value 可查看十六进制，首版不增加二进制 Key 操作。

一次前端搜索最多自动续查 30 秒或 1,000 次 IPC，批次间让出至少 20 ms；在途请求仍受插件超时约束。预算达到后保留结果与游标，明确提示可以继续，不能解释成查无结果。隐藏期间停止续查，返回工作区后由用户选择继续。

有关键词时，后端每次 IPC 最多合并 8 条 SCAN，批次间检查 100 ms 工作预算；单条命令仍使用插件约束后的 COUNT，整次请求仍共用原有超时。SCAN 的 MATCH 始终使用已登记范围，再以搜索模式进一步过滤，用户输入不能扩大授权范围。搜索匹配采用有界迭代算法，不将用户输入转换为回溯正则。

Redis 的 COUNT 是提示而非严格条数保证，扫描超额余项在主进程暂存后续展示，不截掉结果后推进游标。游标有效期五分钟，绑定窗口、作用域、配置修订、连接对象、查询和数据类型；不允许跨查询续页。前端对重复 Key、字段、成员去重。缓存预算合计 16 MiB，其中主进程余项占 4 MiB，Renderer 浏览数据占 12 MiB；这不是进程总内存限制，协议解析与界面渲染仍有临时内存开销。

每插件最多一个桌面读取请求，整次请求共用插件超时。独立 RESP2 通道使用已验证主路由端点和相同认证、DB、TLS 参数，不创建或替换主路由。解析阶段检查声明长度和累计回复，单次回复最大 1 MiB，并限制数组节点和嵌套深度。超限、超时或格式错误关闭该读取通道，不关闭插件主连接；不会将失败伪装为没有数据。

## 桌面接口与安全边界

新增 `redisWorkspaceScan`、`redisWorkspaceInspect`、`redisWorkspaceRead`、`redisWorkspaceRelease` 四个 preload API，沿用 `IpcResult`。数据请求携带项目、环境、插件及 `patternId`，不接受凭据、地址、任意命令或切库参数。

IPC 校验可信主窗口及主 frame；服务层检查完整作用域、当前连接、配置状态和内置操作授权，操作前后均复核会话。窗口及工作区释放凭证还覆盖等待审计时的竞态，旧请求不能在释放后重新建立读取通道。读取共用既有用户操作审计；开始审计失败时不访问 Redis，完成审计失败返回 `auditWarning`。审计不保存 Key、Value、字段、搜索词或服务端错误正文。

上述浏览接口是桌面人工只读能力，不增加 MCP 工具，不扩大 Agent 的 Redis 读取能力，不修改工作区 YAML 格式或凭据模型。内容仅驻留内存，复制只在用户明确点击时发生。

## 验证

```powershell
node --test test/redis-value-format.test.mjs test/redis-key-tree.test.mjs test/redis-key-search.test.mjs test/redis-workspace.test.mjs test/redis-plugin-runtime.test.mjs
corepack pnpm run check
corepack pnpm test
corepack pnpm run test:ui
corepack pnpm run test:ui:redis
```

Redis 专项测试使用本机回环 RESP2 fixture，验证分页余项、游标隔离、字节限制、超时、关闭竞态、审计和主连接隔离。Electron 专项使用隔离数据根与 mock IPC，验证搜索历史复用与清空、目录前缀搜索、自动续扫、匹配结果分页、停止与续查、换词隔离、隐藏暂停、预算耗尽，以及入口、五种类型、复制、主题、分栏及会话清理。正式包 UI smoke 另以回环 Redis fixture 验证真实 preload、IPC、认证、跨空批次通配符搜索、读取和工作区入口，不连接生产基础设施。


## 新增、修改和删除

左侧顶部及空状态提供「新增 Key」，右侧详情提供「编辑值」「删除 Key」。新增在固定标签中填写 Key、文本/JSON 和有效期；默认永久保存，不覆盖同名 Key。编辑在原详情区完成，Key 名称不可改；默认保留原过期时间，保存不会重新计时，也可以明确设置永久或相对时长。JSON 校验不改变原文或大整数。完整 UTF-8 文本最多 64 KiB，插件更低限制仍有效；二进制、截断或超限值保持只读。

保存一次点击即可完成。编辑标签自动固定并标记未保存；关闭、取消编辑、刷新当前内容或切换范围时统一保护草稿。断线、过期及冲突保留输入；「重新读取并核对」对照服务器当前值和草稿，经用户核对后建立新基准。提交结果未知时只允许检查状态，不自动重试。

String、Hash、List、Set、ZSet 均支持单 Key 删除。确认框显示完整名称、类型及 DB；有本地草稿时在同一框说明丢弃。标签关闭、键盘 Delete 和目录操作不会删除服务端数据。

桌面新增 `redisEditOpen/Prepare/Commit/Status/Release` 五个 API。Open 接收固定作用域、patternId、完整 Key 和 create/update/delete；Prepare 接收 editId、原文 value、text/json 格式、过期方式 keep/persistent/relative（毫秒）；删除不接受新值。Commit/Status 只接受 editId 与 planId。所有接口拒绝任意命令、凭据、地址和切库参数。

独立有界 RESP2 写入连接绑定当前主连接代次，WATCH 从读取编辑基准或打开删除确认时开始；MULTI/EXEC 单次消费精确计划，SET NX 防新增覆盖、XX 防已过期 Key 重建，KEEPTTL 保留过期时间，UNLINK 删除单 Key。连接中断、并发修改、过期和驱逐导致冲突或结果未知。编辑会话 30 分钟，保存计划 2 分钟，后端最多 24 个编辑会话。审计只记录范围、操作和结果，不记录 Key、Value 或原始服务端错误。Agent/MCP 继续只读。

最低支持 Redis 6.0.9（含 WATCH 对过期的保护）；建议使用受维护的 Redis 版本。参考官方 [SET](https://redis.io/docs/latest/commands/set/)、[事务](https://redis.io/docs/latest/develop/using-commands/transactions/) 和 [UNLINK](https://redis.io/docs/latest/commands/unlink/) 文档。

验证：`node --test test/redis-data-editing.test.mjs test/redis-workspace.test.mjs`、`corepack pnpm run test:ui:redis`。授权服务器隔离验证使用 `node scripts/database-write-live-probe.mjs`，必须通过环境变量 `RUNBOOK_TEST_HOST`、`RUNBOOK_TEST_PASSWORD`（可选 `RUNBOOK_TEST_USER`）显式提供授权连接，并已在本机 known_hosts 信任主机密钥。脚本复用已安装 MySQL 8.4 镜像及 Redis 可执行文件启动临时回环实例，通过 SSH 隧道验证并清理实例，不访问业务数据，不保存凭据。
