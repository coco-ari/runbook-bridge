# 版本变更记录

本项目使用语义化版本号。正式发布版本以 Git Tag 和 GitHub Release 为准。

## 未发布

### Agent 日志检索

- `server_search_logs` 现在可以直接接收日志文件、目录或日志数据源，由 Agent 在有界范围内递归发现普通日志和轮转归档，不再依赖预先列出每个 `fileId`。
- 一次调用支持最多 10 个字面量条件及 `any/all` 组合，单次读取即可返回匹配行和前后文，避免为不同关键词反复扫描同一个远端大文件。
- `.gz` 与 `.zip` 在应用内存中有界展开并直接搜索，不落盘、不调用 Shell、不需要下载后本地解压；危险条目、嵌套归档和异常压缩比会安全跳过或拒绝。
- 远端日志快照按路径、大小和修改时间短时缓存，并返回覆盖范围、缓存命中、跳过项和截断原因，便于 Agent 判断结果是否完整。

### 版本一致性

- MCP 初始化握手的版本改为直接读取 `package.json`，避免运行时版本与安装包版本漂移。
- README 当前代码包版本和安装升级回归脚本由自动化测试约束，历史发布条目保持原样。
- 发布工作流在安装依赖和构建前校验 Git tag 与 `package.json` 版本完全一致，阻止错版本产物发布。

### Agent 分页契约

- Server 与 MySQL 分页游标在兼容旧版非负整数输入的同时，统一以十进制字符串返回 `nextCursor`；Agent 应原样透传该字符串。
- Redis SCAN 游标保持十进制字符串契约。所有分页运行时现在拒绝非法格式、负数、小数和不安全整数，不再静默回退到第一页。

### 旧入口清理

- 删除当前主进程、MCP 与 V2 UI 不再引用的旧 MCP、旧 renderer、SSH `ConnectionManager` 和持久草稿服务，避免继续进入安装包或误导 Agent。
- 删除只服务旧 UI 的孤立 smoke 与 screenshot 脚本；保留现行 V2 UI、package 验证和 packaged MCP smoke。
- 保留升级仍需要的旧项目/凭据读取和 promotion journal，并将旧 `ProjectStore` 默认 README 更新为 `agent-ops` / `mcp-v2`。

## 1.0.28 - 2026-08-17

### Agent 文件查询性能

- `server_find_files` 在一次查询内复用同一个 SFTP 只读会话，并将目录元数据读取限制为最多 2 路并发，减少远程服务器往返等待。
- `server_search_files` 在文件发现与内容检索阶段复用同一个 SFTP 只读会话，不新增 MCP 接口，也不改变现有参数结构。
- 越南测试服务器实测：遍历 8 个目录、410 个条目的耗时中位数由 15.61 秒降至 6.58 秒，约提升 58%。
- 完整 166 项单元测试通过；升级安装继续保留项目配置和已保存密码。

## 1.0.23 - 2026-08-17

### MCP 与连接运行时

- 修复 Server 日志/配置目录调用在进入 SFTP 前因路径规范化函数缺失而立即返回 `INTERNAL_ERROR` 的问题。
- Server 文件操作统一使用有界 SFTP 生命周期，目录不存在、权限不足、SFTP 不可用和操作超时现在返回可操作的稳定错误，不再泄漏模糊内部错误。
- MySQL 查询超时或连接丢失后立即淘汰坏会话、关闭关联 relay，并通知环境连接管理器更新状态，避免界面继续显示假“已连接”。
- 增加 SFTP 子系统异常与 MySQL 查询超时回归；完整 160 项单元测试通过。

## 1.0.22 - 2026-08-17

### 客户端导航

- 左侧侧边栏简化为项目列表，不再同时承载环境树；单击项目统一进入右侧环境概览，避免项目展开状态与环境可见性不同步。
- 环境名称、插件构成、连接状态和连接操作集中到项目概览，项目内多个环境可以在同一页面完整查看和操作。
- 项目仍可直接拖拽排序，并保留连接状态摘要；移除重复的顶部“项目概览”面包屑。
- 修复长项目列表底部“新建项目”遮挡最后一项的问题，按钮固定在独立侧栏底部区域。

### 回归测试

- UI 回归改为验证项目级侧栏、右侧环境概览、项目拖拽持久化以及从概览进入环境的完整交互。

## 1.0.21 - 2026-08-17

### 修复

- 项目展开改为单次原子更新：箭头、可访问状态、`hidden/inert` 与环境列表在同一次点击中同步，不再等待侧边栏异步重绘。
- 展开位于长列表底部的项目时，自动将新显示的环境滚入可视区域，避免已经展开却看不到子环境。
- 保留后台状态刷新后的完整重绘，但不再让它决定本次点击是否立即生效。

## 1.0.20 - 2026-08-17

### 修复

- 重写项目环境树的可见性控制：箭头与环境列表由同一个展开状态直接控制，收起时使用原生 `hidden`，展开时立即移除，不再依赖环境数量、高度变量或 Electron 的内在网格高度计算。
- 增加收起与展开双向几何回归：环境列表必须同时满足可见高度、无 `hidden`、正确 `aria-hidden`，避免再次出现“箭头向下但环境不可见”。
- 使用位置过渡保持整个项目栏折叠时的视觉连续性，可靠显示与平滑折叠不再互相冲突。

## 1.0.19 - 2026-08-17

### 修复

- 修复当前项目已经位于项目概览时，点击侧边栏项目名称不会展开环境的问题。
- 项目名称整行现在是明确的展开入口；小箭头继续用于独立收起或展开。
- 增加“收起当前项目后点击项目名称，环境列表必须实际可见”的安装版 UI 回归测试。

## 1.0.18 - 2026-08-17

### 修复

- 启动桌面窗口前清理 Electron 页面缓存，避免覆盖安装后继续使用旧版侧边栏 CSS 或 JavaScript。
- 保留项目环境树可靠展开方案，并继续由安装版几何回归测试验证环境子项实际可见。

## 1.0.17 - 2026-08-17

### 修复

- 修复安装版中项目箭头已经展开，但环境列表仍被计算为 0 高度、无法点击环境的问题。
- 环境树改为明确的高度展开，并增加安装版样式兜底，不再依赖 Electron 中不稳定的 intrinsic `1fr` 高度计算。
- 增加环境列表真实可见性回归测试，除检查 DOM 数据外，同时检查裁剪区高度、透明度与每个环境行的实际位置。
- 统一侧边栏宽度、连接概览和环境树的过渡时间，降低折叠与展开时的单帧跳动。

## 0.3.2 - 2026-08-14

### 修复

- 修复 SFTP 读取完成或连接意外关闭时，底层未完成请求可能触发 `No response from server`，进而弹出 Electron 主进程 JavaScript 错误的问题。
- SFTP 会话现在统一管理超时、中止、错误监听和通道关闭，确保同一个通道只关闭一次，并持续接收关闭阶段的晚到错误。
- 结构化日志搜索改为显式执行 `open`、分块 `read` 和 `close`，确认远端文件句柄已经关闭后才结束 SFTP 通道。
- 上传、下载或日志搜索过程中断线时返回稳定的 `TRANSFER_INTERRUPTED` 业务错误；下载失败会清理本地临时文件，日志搜索会释放并发状态。

### 升级说明

- 安装 0.3.2 后请完全退出并重新启动 Codex，使桌面端和 MCP 同时加载新版本。
- MCP 注册路径没有变化，通常不需要删除或重新注册 MCP。

## 0.3.1 - 2026-08-14

### 修复

- 修复项目超过约 8 个时，左侧项目列表把“新建项目”和设置按钮挤出窗口的问题。
- 项目列表现在在侧栏内部独立滚动，底部操作按钮始终保持可见。

## 0.3.0 - 2026-08-14

### Agent 使用效率

- MCP 工具由 5 个增加到 7 个，新增 `execute_batch` 和 `search_logs`。
- `execute_batch` 一次可顺序执行 1 到 10 条互不依赖的命令；每条命令仍独立经过上下文校验、危险命令策略、超时和审计。默认遇到策略拒绝、执行异常或非零退出码后停止。
- `search_logs` 通过现有 SFTP 会话读取明确的日志文件，在本机进行字面量 AND/OR 搜索，支持前后文、结果截断说明和短期分页游标。
- 项目文档、SSH 会话和安全配置未变化时，同一 MCP 进程可复用 30 分钟内的 `contextToken`，并返回签发、过期和剩余有效期。

### 连接可靠性

- VPN 切换、网络抖动和临时连接失败会按照 1、2、5、10、30 秒退避自动重连。
- SSH 认证失败后立即停止自动重连，避免错误密码持续尝试导致服务器账号锁定；桌面端会显示“重连已停止”，用户可点击“连接”重新输入凭据。
- 私钥不可用、主机指纹变化、凭据存储不可用和无效代理配置等需要人工处理的错误也不会无限重试。
- 用户主动断开、删除项目或退出程序时立即取消自动重连。

### 安全与审计

- `contextToken` 新增安全配置哈希绑定；危险命令策略或资源限制变化后，旧令牌立即失效。
- 命令执行、上传、下载、日志搜索和自动重连记录统一的 `operationId`、结果和错误状态。
- 审计记录升级为 `schemaVersion: 2`，审计目录和文件使用更严格的本地权限。
- 命令与批量输出返回字节数、上限和截断状态；批量输出接近上限时优先保留错误输出。
- 删除实验性的“会话权限/只读排障”模式。安全边界继续保持简单明确：专用低权限 Linux 账号是主边界，危险命令策略只用于减少明显误操作。

### 日志搜索限制

- 一次最多 10 个文件和 10 个关键词。
- 默认扫描每个请求涉及文件的尾部数据，总扫描默认上限 16 MiB、硬上限 32 MiB。
- 单项目最多一个并发搜索，全局最多两个。
- 只支持字面量匹配，不支持正则表达式、时间语义或跨行事件解析。
- 日志路径推荐记录在项目 Markdown 中，但真正的读取权限由 Linux SSH 账号决定，Markdown 不是文件访问白名单。

### 兼容性与升级

- 原有 `list_projects`、`open_project`、`execute`、`upload`、`download` 保持可用。
- 项目目录、Markdown、加密凭据和已固定的服务器指纹无需迁移。
- 旧项目缺少的新配置会使用安全默认值；实验版本遗留的 `access`、`diagnostics` 字段会被忽略，并在下一次保存项目时清理。
- 审计记录增加字段；严格解析旧 JSON 结构的外部脚本需要兼容 `schemaVersion: 2`。
- 安装后必须完全退出并重新启动 Codex，使桌面端和 MCP 同时加载 0.3.0。MCP 注册路径没有变化，通常不需要重新注册。

### 使用注意

- `execute_batch` 不是事务，前面的命令成功、后面的命令失败时不会自动回滚。部署回滚步骤仍应写入项目文档，并由 Agent 根据结果执行。
- SSH 命令超时或网络中断不能证明服务器端子进程一定终止；对有副作用的操作不要盲目重试。
- 上传后运行的 JAR 或脚本拥有 Linux 登录账号本身的权限。不要连接 `root`，不要给项目账号任意 `sudo`。

## 0.1.8 - 2026-08-12

- 增加 SSH 意外断线后的退避自动重连。
- 用户主动断开后取消后续重连。

## 0.1.7 - 2026-08-12

- 首个稳定公开版本。
- 提供多项目、Markdown 操作文档、密码/私钥/SSH Agent、SOCKS5/HTTP CONNECT 代理、主机指纹固定、文件上传下载和 5 个基础 MCP 工具。
- 提供 Windows DPAPI 凭据保存、文档与 SSH 会话绑定的 `contextToken`、危险命令拦截和 JSON Lines 审计。
